spring:
      data:
          mongodb:
            uri: mongodb://${OPENSHIFT_MONGODB_DB_HOST}:${OPENSHIFT_MONGODB_DB_PORT}/${OPENSHIFT_APP_NAME}
            username: ${OPENSHIFT_MONGODB_DB_USERNAME}
            password: ${OPENSHIFT_MONGODB_DB_PASSWORD}

      mail:
          properties.mail.smtp.ssl.enable: true
          host:
          username:
          password:


mailConfig:
      defaultFrom:
      subject: New vacancy

logging:
      level.org.springframework: ERROR
      level.org.hibernate: ERROR
      level.org.apache.http: ERORR

security:
      ignored: /**
      basic:
          enabled: false


management:
      security:
          enabled: false

queue:
      use: false

job:
      cron: "0 0/10 * * * *"
      query: qa






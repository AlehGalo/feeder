spring:
      data:
          mongodb:
            uri: mongodb://admin:9HKrhYfEzbNq@localhost:27017/tomcat

      mail:
          properties.mail.smtp.ssl.enable: true

mailConfig:
      defaultFrom:
      subject: New vacancy

logging:
      level.org.springframework: ERROR
      level.org.hibernate: ERROR
      level.org.apache.http: ERORR

security:
      ignored: /**
      basic:
          enabled: false


management:
      security:
          enabled: false

queue:
      use: false

job:
      cron: "0 0/10 * * * *"
      query: qa